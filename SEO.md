# SEO

## Source of truth

- Status: Active
- Last refreshed: 2026-09-02
- Evidence reviewed: `https://yejilog-mu.vercel.app` 배포본 실측(HTTP 응답, SSR HTML, Chrome headless 렌더), `app/sitemap.ts`, `app/page.tsx`, `components/home/InfinitePostLoader.tsx`, `utils/posts.ts`, 커밋 이력 `2d6d3d7..7897bf3`
- 측정 도구: `npm run check:pages` (실제 브라우저로 전 페이지 렌더 검사)

## 왜 이 문서가 있나

이 블로그는 두 번 검색 문제를 겪었다. 둘 다 "코드가 잘못됐다"기보다 **크롤러가 사이트를 어떻게 보는지가 사람이 보는 것과 달랐던** 문제였다. 같은 실수를 반복하지 않도록 판단 근거를 남긴다.

---

## 문제 1 — 검색 결과에 "제목없음 + Application error"

### 증상

구글에서 블로그를 검색하면 제목이 **"제목없음"**, 설명이 `Application error: a client-side exception has occurred while loading yejilog-mu.vercel.app` 로 떴다.

### 진단

`2026-03-09` → `2026-07-06` 사이 **4개월간 배포가 없었다.** 구글이 그 기간에 크롤링했고, 당시 버전은 클라이언트 예외로 죽었다. error boundary가 없어 Next.js 기본 에러 화면이 그대로 노출됐고, **그 화면에는 `<title>`이 없다.** 검색 결과의 "제목없음"이 정확히 이것이다.

현재 배포된 41개 URL은 실제 브라우저 렌더 결과 전부 정상이다. 즉 페이지는 이미 고쳐져 있었고, 남아 있던 것은 구글이 들고 있는 옛 스냅샷이었다.

문제는 **그 스냅샷이 갱신되지 않는 구조**였다. `app/sitemap.ts`가 `lastModified`를 **글 작성일**로 내보내고 있었다:

```
<lastmod>2026-03-03T19:35:18.000Z</lastmod>   ← 8월에 전면 리뉴얼했는데도 3월로 신고
```

크롤러에게 "이 페이지는 변경 없음"이라고 계속 말하는 셈이라 재색인 우선순위가 올라가지 않는다.

### 선택한 대응과 근거

| 선택 | 근거 |
|---|---|
| `app/error.tsx` + `app/global-error.tsx` 추가 | 예외를 없애는 것보다 **예외가 나도 검색에 해롭지 않은 화면**이 나가는 게 먼저다. 예외는 언제든 다시 날 수 있다. 두 파일 모두 `<title>`과 `noindex`를 직접 심는다 |
| `global-error`는 인라인 스타일로 자기 완결 | 루트 레이아웃까지 무너진 상황이 발동 조건이다. 그 화면이 `globals.css`나 폰트에 의존하면 안 된다 |
| `lastModified`를 `updated` → mtime → 빌드 시각 순으로 | 작성일은 "콘텐츠가 언제 쓰였나"이고 크롤러가 알아야 할 것은 "페이지가 언제 바뀌었나"다. 서로 다른 값이다 |
| 레이아웃에서 전역 `index/follow` 제거 | 404·에러 화면의 `noindex` **뒤에** 그대로 따라붙어 상충하는 robots 메타가 나갔다. 지정하지 않는 것이 곧 `index, follow`이므로 명시할 이유가 없다 |
| `scripts/checkRenderedPages.mjs` 신설 | **HTTP 200과 정상 렌더는 다른 문제다.** SSR HTML이 멀쩡해도 hydration이 깨지면 화면은 에러로 바뀐다. curl로는 절대 잡히지 않는다 |

### 검사 스크립트가 error boundary도 실패로 보는 이유

`CRASH_MARKERS`에 우리 error boundary의 표식(`render-error-page`, `data-render-error`)도 넣었다. boundary가 화면을 대신 그려주는 것은 **사고를 감춘 것이지 고친 것이 아니다.** 검사는 사고 자체를 봐야 한다.

---

## 문제 2 — 글 38개 중 일부만 색인

### 증상

`site:yejilog-mu.vercel.app` 검색에 13개만 나왔다.

> **주의:** `site:` 결과 수는 구글의 대략적 추정치이고 실제 색인 수보다 적게 나오는 경우가 흔하다. 실제 수치는 Search Console → 페이지 리포트의 "색인 생성됨"에만 있다. 개선 폭을 판단할 때는 `site:`가 아니라 Search Console을 기준으로 본다.

다만 아래 구조적 문제는 `site:` 수치와 무관하게 실재했다.

### 진단

**색인 가능한 글 목록 페이지가 홈 하나뿐이었고, 그 홈은 최신 5개만 보여줬다.**

| 경로 | 크롤러가 보는 것 |
|---|---|
| 홈 `/` | 최신 5개만 SSR. 나머지는 무한 스크롤(JS)로만 로드 |
| 다음 페이지 `?limit=10` | **`<a>` 링크가 아님** — `router.replace()` JS 호출뿐 |
| 카테고리 `?category=CSS` | 링크는 맞지만 canonical이 전부 `/`. 구글이 홈의 중복으로 보고 색인하지 않으며, 여기도 5개까지만 |
| 글 페이지 | 내부 링크가 이전·다음 글 1~2개뿐. category·tag pill은 `<span>`이라 링크가 아니었음 |

`/?limit=40`을 직접 요청하면 38개가 전부 SSR로 나온다. **경로는 있는데 아무도 그 URL을 링크하지 않았다.**

도달 가능성 계산:

```
React&JS         15개  → 10개는 링크로 도달 불가
infrastructure    6개  →  1개는 링크로 도달 불가
홈 + 카테고리 링크로 도달: 27 / 38
이전·다음 체인에만 의존:  11개  (체인 깊이 최대 19단계)
```

**sitemap은 "발견"을 돕지만 색인을 보장하지 않는다.** 내부 링크가 없으면 구글은 중요하지 않은 페이지로 판단하고 "발견됨 – 색인 생성되지 않음"에 방치한다.

### 선택한 대응과 근거

핵심 원칙: **모든 글이 홈에서 두 단계 안에 들어오고, 글마다 나가는 링크가 여러 개 있어야 한다.**

| 선택 | 근거 |
|---|---|
| **`/archive` 신설** — 전체 38개를 한 화면에 | 가장 적은 변경으로 도달 불가 문제를 완전히 없앤다. 글이 늘어도 이 페이지 하나가 계속 커버한다. `priority: 0.9`로 sitemap에서 홈 다음으로 높게 둔다 |
| **`/categories/[category]` 신설** — 카테고리별 **전체** 목록 | `?category=`는 canonical이 `/`로 묶여 색인되지 않는다. 색인 가능한 자기 canonical을 가진 허브가 필요했다. React&JS 15개처럼 큰 카테고리의 나머지 글이 여기서 링크된다 |
| 쿼리 파라미터 필터(`?category=`, `?limit=`)는 canonical `/` **유지** | 같은 목록을 여러 URL로 색인시키면 중복 콘텐츠가 된다. 필터는 사람이 쓰는 도구로 남기고, 색인은 `/archive`와 `/categories/*`가 담당하게 역할을 나눴다 |
| 글 상세의 category·tag pill을 링크로 | 글 하나에서 나가는 링크가 이전·다음뿐이면 크롤러가 목록으로 되돌아갈 길이 없다. 글 38개 × 링크 몇 개가 그대로 내부 링크 밀도가 된다 |
| `InfinitePostLoader`에 `<a>` 폴백 추가 | 스크롤 관측은 크롤러와 JS 없는 환경에서 동작하지 않는다. **같은 목적지를 링크로도 내보내면** 무한 스크롤 UX를 그대로 두면서 크롤 경로가 생긴다 |
| 푸터에 Archive 링크 | 어느 글에서 시작해도 전체 목록에 닿는다. 헤더가 아니라 푸터로 둔 것은 모바일 내비 레이아웃을 건드리지 않기 위해서다 |
| 태그 페이지(`/tags/[tag]`)는 **만들지 않음** | 태그 94개 중 **71개가 글 1개짜리**다. 내용이 얇은 허브 페이지를 대량 생산하면 오히려 저품질 페이지로 취급된다. 태그는 `?tags=` 필터로 남긴다 |
| 태그 링크에 `rel="nofollow"` | 태그 필터는 canonical이 `/`로 묶여 **색인 가치가 0인데 크롤 예산은 쓴다.** 94개 URL을 크롤하게 두면 archive·카테고리·글에 갈 예산이 줄어든다. 사람에게는 그대로 열어 두고 크롤만 막았다. 카테고리 pill은 색인 대상(`/categories/*`)이라 nofollow를 붙이지 않는다 |

### 왜 헤더가 아니라 푸터인가

Archive를 헤더 내비에 넣으면 도달성은 조금 더 좋아지지만 `POSTS / PROJECTS / ABOUT` 3개 구조와 모바일 레이아웃을 함께 손봐야 한다. 푸터는 모든 페이지에 이미 있어 도달성 목표를 동일하게 달성하면서 디자인 리스크가 없다. 홈에는 목록 하단에 전체 글 링크를 따로 두어 가장 자연스러운 지점에서도 이어지게 했다.

---

## 반복하지 말 것

1. **`site:` 검색 수를 색인 지표로 쓰지 않는다.** Search Console 페이지 리포트를 본다.
2. **HTTP 200을 정상으로 믿지 않는다.** 배포 전 `npm run check:pages`로 실제 렌더를 확인한다.
3. **JS로만 도달하는 목록을 만들지 않는다.** 무한 스크롤·필터를 쓰더라도 같은 목적지로 가는 `<a>`를 함께 남긴다.
4. **`lastModified`에 작성일을 넣지 않는다.** 크롤러가 알아야 할 것은 페이지가 바뀐 시점이다.
5. **전역 metadata에 `index/follow`를 명시하지 않는다.** 404·에러 화면의 `noindex`와 충돌한다.
6. **글을 옮기거나 파일명을 바꿀 때는 리다이렉트를 함께 넣는다.** 과거 `?` 포함 파일명을 정리한 이력이 있고, 그때 기존 URL은 그대로 끊겼다.

## 배포 후 해야 할 일

sitemap과 링크를 고쳐도 크롤러가 다시 오기 전까지 검색 결과는 그대로다.

1. Search Console → **URL 검사** → 해당 URL → **색인 생성 요청**
2. Search Console → **Sitemaps** → `sitemap.xml` 재제출
3. 2~4주 후 **페이지 리포트**에서 "색인 생성됨" 추이와 미색인 사유를 확인

## 참고: Vercel의 mtime

Vercel은 git clone으로 빌드하므로 **파일 mtime이 clone 시각**이 된다. 즉 `lastModified`는 실질적으로 "배포 시각"으로 동작한다. 글별 정확한 수정 시각이 필요하면 frontmatter에 `updated`를 넣는다. 그 값이 최우선으로 쓰이고 `validatePosts.cjs`가 형식을 함께 검사한다.

```yaml
---
title: 글 제목
date: 2025-03-18 18:06:00
updated: 2026-09-02
---
```
